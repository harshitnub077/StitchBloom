{
    "name": "@crochetverse/database",
    "version": "0.0.0",
    "main": "./src/index.ts",
    "types": "./src/index.ts",
    "scripts": {
        "build": "tsc",
        "clean": "rm -rf dist .turbo node_modules",
        "db:generate": "prisma generate",
        "db:push": "prisma db push",
        "db:seed": "prisma db seed"
    },
    "prisma": {
        "seed": "ts-node --compiler-options {\"module\":\"CommonJS\"} prisma/seed.ts"
    },
    "dependencies": {
        "@prisma/client": "^5.10.2"
    },
    "devDependencies": {
        "@crochetverse/config": "workspace:*",
        "prisma": "^5.10.2",
        "ts-node": "^10.9.2",
        "typescript": "^5.3.3"
    }
}